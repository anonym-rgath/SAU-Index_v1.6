# Rheinzelmänner Konfiguration
# ============================
# Kopiere diese Datei zu ".env" und passe die Werte an:
#   cp .env.example .env

# Port auf dem die App läuft (Standard: 80)
APP_PORT=80

# JWT Secret für Token-Verschlüsselung
# WICHTIG: Generiere einen neuen Wert mit: openssl rand -hex 32
JWT_SECRET=change-this-to-a-secure-random-string

# Admin Passwort beim ersten Start
# WICHTIG: Ändere das Passwort nach dem ersten Login!
ADMIN_PASSWORD=admin123
